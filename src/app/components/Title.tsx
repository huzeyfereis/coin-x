import { Typography } from '@mui/material';

const Title = () => {
  return (
    <div>
      <Typography variant='h4' marginBottom={2}>
        Coin - X
      </Typography>
    </div>
  );
};

export default Title;
